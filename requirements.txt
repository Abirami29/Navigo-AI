# Also copied to src/requirements.txt — Databricks Apps require a
# requirements.txt at the root of source_code_path (src/) to install
# anything beyond a small pre-installed set. Update both files together.
requests>=2.31
psycopg>=3.1
python-dotenv>=1.0
streamlit>=1.35
pandas>=2.2
tenacity>=8.2
pytest>=8.0
databricks-vectorsearch>=0.40
databricks-sdk>=0.20.0
